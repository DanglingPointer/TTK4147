#include<iostream>

void allocate(int value, long long i)
{
  int *ptr = new int[100000];
  *ptr = value;
  std::cout << "Test of allocated memory: " << i << std::endl;
}

int main()
{
  long long i = 0;
  for(;;){
    allocate(55, i++);
  }
  return 0;
}
